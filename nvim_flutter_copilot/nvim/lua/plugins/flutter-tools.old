return {
	"akinsho/flutter-tools.nvim",
	fvm = {
		-- The path to your fvm config file
		config_file = "~/.fvm/fvm_config.json",
	},
	debugger = {
		enabled = true,
		run_via_dap = true,
		register_configurations = function(_)
			local dap = require("dap")
			-- dap.adapters.dart = {
			-- 	type = "executable",
			-- 	command = "node",
			-- 	args = { debugger_path, "flutter" },
			-- }
			dap.set_log_level("TRACE")
			dap.configurations.dart = {}
			require("dap.ext.vscode").load_launchjs()
		end,
	},

	-- not using as fvm  is not installed
	--flutter_path = "/usr/bin/flutter",
	widget_guides = {
		enabled = true,
	},
	closing_tags = {
		highlight = "ErrorMsg",
	},
	dev_log = {
		open_cmd = "tabnew",
	},
	outline = {
		open_cmd = "30vnew",
	},
	lsp = {
		settings = {
			showTodos = true,
			completeFunctionCalls = true,
		},
	},
	-- This is the default config for the plugin

	lazy = false,
	dependencies = {
		"nvim-lua/plenary.nvim",
		"stevearc/dressing.nvim", -- optional for vim.ui.select
	},
	config = true,
}
